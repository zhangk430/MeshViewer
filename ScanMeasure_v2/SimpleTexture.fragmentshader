#version 330 core

// Ouput data
layout(location = 0) out vec4 color;

uniform sampler2D texture;
uniform sampler2DShadow shadowMap;

in vec2 UV;

void main(){
	color = texture(texture, UV) + texture(shadowMap, vec3(UV, 1));
}